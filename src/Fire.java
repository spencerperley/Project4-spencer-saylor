import processing.core.PImage;

import java.util.List;

public class Fire extends AnimatedEntity{
    public Fire(String id, Point position, int animationPeriod, List<PImage> images) {
        super(id, position, animationPeriod, images);
    }
}
